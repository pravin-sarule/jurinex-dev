# citation-service-v1 dependencies
# Google ADK (Agent Development Kit) — agent orchestration framework
google-adk>=1.0.0

# LiteLLM — routes Google ADK LlmAgent calls to Claude (Anthropic)
litellm>=1.40.0

# Anthropic Claude SDK — direct LLM calls in tools/utils
anthropic>=0.40.0

# FastAPI + ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# HTTP client — async requests to Serper, IK, document-service, web fetch
httpx>=0.27.0

# Database
asyncpg>=0.29.0          # PostgreSQL async driver

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Environment
python-dotenv>=1.0.0

# Logging
rich>=13.7.0

# Utilities
python-dateutil>=2.9.0
