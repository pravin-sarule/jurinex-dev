# Ingestion pipeline (GCS, Document AI, embeddings, DB)
# Copy to .env and fill values. Same DB/GCS as document-service.

# Google API key (Gemini embeddings + optional ADK)
GOOGLE_API_KEY=
GEMINI_API_KEY=

# Document_DB: user_files, file_chunks, chunk_vectors, cases (same as document-service)
# Use DOCUMENT_DATABASE_URL or DATABASE_URL — content is always fetched from this DB.
DOCUMENT_DATABASE_URL=postgresql://user:password@localhost:5432/document_db
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# GCS (same buckets as document-service)
GCS_BUCKET_NAME=
GCS_INPUT_BUCKET_NAME=
# Optional: base64-encoded service account JSON (or use GOOGLE_APPLICATION_CREDENTIALS / gcs-key.json)
# GCS_KEY_BASE64=

# Document AI (same as document-service)
GCLOUD_PROJECT_ID=
DOCUMENT_AI_LOCATION=us
DOCUMENT_AI_PROCESSOR_ID=

# Embedding (optional overrides)
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
GEMINI_EMBEDDING_DIMENSIONS=768
GEMINI_EMBEDDING_MAX_CHARS=8000
GEMINI_EMBEDDING_BATCH_SIZE=100

# Run mode: adk (use ADK + local ingestion) | mock (no ADK, mock agents)
JURYNEX_MODE=adk

# JWT (same secret as authservice; required for user-specific ingest/retrieve)
# All agent flows use user_id from JWT only—no random chunks, document-specific per user.
JWT_SECRET=

# Draft_DB (templates, template_assets, template_css, template_html, template_images)
# Used by template gallery API: GET /api/templates, GET /api/templates/{id}, GET /api/templates/{id}/preview-image
DRAFT_DATABASE_URL=postgresql://user:password@localhost:5432/draft_db

# CORS allowed origins (comma-separated). Frontend must be listed for template gallery to work from browser.
# Default: http://localhost:5173 (Vite), http://localhost:3000
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
